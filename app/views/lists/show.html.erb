<h1><%= @list.name %></h1>

<% @list.movies.each do |movie| %>
  <h2><%= movie.title %></h2>
  <p><%= movie.overview %></p>
  <%= link_to "delete", bookmark_path(bookmarks), data: {turbo_method: :delete}%>
<% end %>

<div>
  <%= link_to "Edit this list", edit_list_path(@list) %> |
  <%= link_to "Back to lists", lists_path %>
  <%= link_to "Destroy this list", @list, method: :delete %>
</div>
